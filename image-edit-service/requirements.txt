# Qwen-Image-Edit-2511 Service - Self-hosted with 24GB GPU
# Model: toandev/Qwen-Image-Edit-2511-4bit (pre-quantized)
# Output: 768x1024 portrait for mobile

# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Image Processing
Pillow==11.1.0
numpy==2.2.1

# PyTorch (CUDA 12.x)
torch==2.5.1
torchvision==0.20.1

# Diffusers - install from git for QwenImageEditPlusPipeline support
# pip install git+https://github.com/huggingface/diffusers
diffusers @ git+https://github.com/huggingface/diffusers
transformers>=4.48.0
accelerate>=1.3.0
safetensors>=0.5.2

# Quantization support (for pre-quantized model loading)
bitsandbytes>=0.45.0

# PEFT for LoRA support (Lightning LoRA)
peft>=0.14.0

# Background removal
rembg==2.0.63
onnxruntime-gpu==1.20.1

# Utilities
httpx==0.28.1
python-dotenv==1.0.1
pydantic==2.10.5
pydantic-settings==2.7.1
